# Week 4: Method Comparison and Accuracy

This lab runs FTCS, BTCS, and Crank-Nicolson on the same 1D heat-equation problem. It compares the final profile, error history when an exact solution is available, stability warnings, and runtime.

Edit `question_config.py`, then run `00_run_comparison_lab.py`.
